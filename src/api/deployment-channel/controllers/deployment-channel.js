'use strict';

/**
 * deployment-channel controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::deployment-channel.deployment-channel');
