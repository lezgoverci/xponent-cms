'use strict';

/**
 * foundation-model controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::foundation-model.foundation-model');
